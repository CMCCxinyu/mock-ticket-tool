services:
  - type: web
    name: mock-ticket-tool
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn tool_server:app --host 0.0.0.0 --port $PORT
    plan: free